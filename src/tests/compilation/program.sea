c block:
    #include <stdio.h>

    int main() {
        printf("This is the beginning of a real Sea test!\n");

        int i = 0;
        int j = 4;

/* Be sure to declare all vars in a c block, since
    Sea does not understand vars yet */

for i; i < 20; ++i:
    c block:
        printf("%d ", i);

while &i != &i:
    pass
    // This is never true

do:
    c block:
        printf("%c ", 'a' + j);
while j > 10

do:
    do:
        do:
            c block:
                printf("1");
        while false
        c block:
            printf("2");
    while false
    c block:
        printf("3");
while false
c block:
    printf("4");

c block:
        printf("\n");

        return 0;
    } // Close main
